{
  "name": "specloop-loop",
  "version": "0.1.0",
  "description": "CLI-agnostic loop orchestrator generated by specloop into a scaffolded repo. Reads docs/roadmap.md and works through each spec's tasks.md.",
  "type": "commonjs",
  "bin": {
    "loop": "./bin/loop.js"
  },
  "dependencies": {
    "tsx": "^4.19.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
