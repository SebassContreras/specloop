# Architecture

## Container

Single-file Node.js script (`hello.js`), run via `node hello.js`. No build step, no
dependencies.

## Conventions

None — deliberately trivial fixture.

## Still to define

N/A — fixture only, not a real project.
