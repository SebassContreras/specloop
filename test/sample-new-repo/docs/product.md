# Product

## What this is

A tiny demo CLI (`hello-cli`) that prints a greeting. Deliberately trivial — chosen
so that, *if* a loop existed to execute it, the result would be easy to verify by
hand.

## Who uses it

Nobody — synthetic fixture used only to test the specloop plugin's own scaffold +
Q&A mechanics.

## Out of scope

Everything except printing a fixed greeting.
