# 001 — hello-cli

## Requirements (smoke-test fixture)

- Running `node hello.js` prints `Hello, world!` to stdout and exits 0.

## Out of scope

- Arguments, config, i18n — anything beyond the single fixed greeting.
