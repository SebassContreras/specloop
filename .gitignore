node_modules/
dist/
# test/ holds throwaway repos used to exercise the skills end-to-end —
# local-only, never pushed.
test/
# .specloop/ itself is committed: loop.config.json is the project's tracked
# loop default, and interview.md is the resumable interview ledger. Only
# .specloop/logs/ (per-run worker logs) stays untracked — see its own
# .specloop/.gitignore, written by skills/start.
