# 018 — project-style-preferences — Tasks

Status legend: `todo` · `in_progress` · `blocked` · `interrupted` · `done`
Owner: `agent` (loop-runnable) · `human` (skipped by the loop)

- [x] T001 [human] [status:done] Live-verify capture → storage → delivery end to end under a real, non-Claude-Code harness, against a throwaway fixture with a visual surface
      └─ Run 2026-09-13, OpenCode, `style-verify-fixture/` (own `.git`, outside this repo, `.agents/skills/` copy — same pattern as `022` T003's audit). Phase 5 Q&A: all 10 dimensions ended `covered`/`skipped` (`brand-refs` skipped with reason), two-consecutive-clean-sweep closing confirmed, `help-me-decide` (`016`) fired correctly for `palette`/`typography` with real web search and 3-4 ranked options each, `preference-strength` asked and recorded (`hard rule` for everything). `planning/styles.md` + `AGENTS.md`'s Style section verified on disk (not just the transcript — ruled out a PowerShell console-encoding scare as display-only, real files were correct UTF-8). Delivery: ran `specloop:loop` on a one-task spec (`001-sample-widget`); native OpenCode sub-agent picked up `contextFiles` including `planning/styles.md`, and the produced `src/Badge.tsx` genuinely honored every hard rule (exact hex tokens, Geist font stack + fallback, 6px radius, computed contrast ratios ≥4.5:1, aria-label, no excess animation). Roadmap rolled up to `done` correctly.
      └─ Found and fixed two real gaps along the way (not this spec's own defects, but surfaced by its verification): `planning/fix/001-language-field-format` (no format specified for `loop.config.json`'s `language` field — two fixtures disagreed; now BCP 47/ISO 639-1 lowercase code, plus a general UTF-8-no-BOM rule for any future coded field) and `planning/fix/002-stage-not-reset-on-done` (`skills/loop`'s roll-up wrote `Status: done` but never reset `Stage` to `—`, contradicting `architecture.md`'s own column definition).
