# 030 — dashboard-build-script — Tasks

Status legend: `todo` · `in_progress` · `blocked` · `interrupted` · `done`
Owner: `agent` (loop-runnable) · `human` (skipped by the loop)

- [x] T001 [agent] [status:done] Write `skills/status/scripts/build_dashboard.py`: read `planning/roadmap.md` and every spec's `tasks.md`, compute task counts and eligibility, detect the five `Stage`/`Status` drift rules, read `planning/fix/`, assemble the JSON contract, substitute it into `skills/status/references/template.html` (escaping `</script` case-insensitively), and write `planning/dashboard.html`. No arguments; runs from the repo root; sorts every listing explicitly and embeds no timestamp for determinism; opens the output with `encoding="utf-8", newline="\n"`; standard library only, no `pip install`.
      └─ Master-verified against disk: runs standalone, matches template.html's exact JSON schema/placeholder, correct data for real rows (030/034/028), no false drift, byte-identical across two runs, UTF-8 no BOM. `generatedAt` deliberately empty (no timestamp) — template already renders blank gracefully.
- [x] T002 [agent] [status:done] Verify the script's output against this repo's own real data, comparing it to what `SKILL.md`'s current Phase 0-5 prose algorithm produces today — same JSON shape, same escaping, and the same results for all five drift rules and the eligibility rule.
      └─ Master-verified: read current `SKILL.md` Phases 0-5 line by line against the script. All 5 drift rules, eligibility, dependsOn parsing, fix-log fields, and `</script` escaping match exactly. Only deviation is the deliberate, already-documented one: `generatedAt` is `""` instead of a real timestamp, required by this spec's own determinism constraint (superseded prose instruction, T005's job to remove). All 5 drift rules are currently inert against this repo's real data (nothing violates any of them right now) — same precedent as `026` T014 for what "verified against real data" means when a rule isn't currently exercised.
- [x] T003 [agent] [status:done] Verify determinism: run the script twice against unchanged input files and confirm byte-identical `dashboard.html` output.
      └─ Master-verified directly: ran twice against this repo's real data, identical md5 and `cmp` confirmed byte-identical. No sub-agent needed — already conclusive.
- [x] T004 [agent] [status:done] Verify the failure path: with `python3` deliberately unavailable (renamed off `PATH` in a throwaway local fixture), confirm `specloop:status` fails with a clear, explicit error naming the missing dependency — no silent fallback, no cryptic stack trace.
      └─ Found the raw shell error is NOT acceptable as-is: Bash gives "python3: command not found" (exit 127), PowerShell gives a locale-dependent CommandNotFoundException with no reliable exit code — neither says what to do, and they're not portably detectable by a single string/exit-code check. T005 must add an explicit dependency check/wrapper in `SKILL.md` reporting "specloop:status requires python3, which was not found on PATH. Install Python 3 and retry." — not just bare-invoke and let the raw error show.
- [x] T005 [agent] [status:done] Rewrite `skills/status/SKILL.md`: replace Phases 0-5 with an instruction to run the script and present its output as the Phase 3 chat summary — no parallel prose description of the algorithm remains.
      └─ Master-verified: read the full rewritten file. 4 phases (run script incl. python3-missing wrapper verbatim / read back JSON / print chat summary from JSON only / report path), no re-parsing of roadmap.md/tasks.md anywhere, same 5-section summary shape preserved. Swept repo for other stale "Phase 0-5" mentions — remaining ones are historical spec docs (026/027/handoff/fix log) describing already-completed work, left alone per existing convention.
- [x] T006 [agent] [status:done] Update `skills/status/references/template.html`'s header comment: reduce the JSON schema description to a pointer at `build_dashboard.py` instead of a duplicate schema description.
      └─ Master-verified: 53-line schema comment reduced to a 5-line pointer, diff scoped only to that block, placeholder token still appears exactly once. Regression-checked: script still substitutes correctly after the edit.
