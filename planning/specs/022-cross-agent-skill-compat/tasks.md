# 022 — cross-agent-skill-compat — Tasks

Status legend: `todo` · `in_progress` · `blocked` · `interrupted` · `done`
Owner: `agent` (loop-runnable) · `human` (skipped by the loop)

- [x] T001 [human] [status:done] Cursor: install specloop's skills, confirm discovery of `skills/<name>/SKILL.md`, confirm auto-trigger from a natural-language request matching a skill's `description` (unmodified frontmatter), confirm the harness tolerates the `when_to_use` key, and run `/specloop:start` far enough to confirm the one-question-at-a-time write-as-you-go loop survives a full phase. Record pass/fail per `design.md`'s audit matrix.
      └─ Agent-driven run, 2026-09-19 — all four checks pass. Cursor CLI 2026.09.18-9a7762b (`cursor-agent`), account tier Free, model Auto; isolated fixtures with their own `.git` and a `skills/` copy in `.agents/skills/`; `cursor-agent -p` with `--trust` (required in headless mode) and `--force` (so it can write files and run commands in the throwaway fixture; not tested without it), conversation continued with `create-chat` + `--resume <id>`; account signed in by the owner. Discovery: pass — asked which skills it sees, it listed all nine from `.agents/skills/` (and also loads the user's `~/.claude/skills` and Cursor's built-ins, one of which is also named `loop`). This settles the open question from step 1: the CLI does load skills. Auto-trigger: pass — "where are we? give me the status of this project's roadmap" made it read `.agents/skills/status/SKILL.md` unprompted, probe `python3` then `python`, run the script by its absolute `.agents/skills/` path and print a correct summary of the same 31998-byte dashboard as the other harnesses; "I need to set up a new project and get it organized from scratch." activated `start`, neither naming a skill. `when_to_use` tolerance: pass — all nine skills load, no warning on stderr or in any output (Cursor exposes no skill debug log, so this is the absence of errors). Multi-turn write-as-you-go: pass — 15 turns; it scaffolded on turn 1 like Codex/OpenCode; every final message held one question except turn 5, which asked one dimension two ways ("how will you know the whole project is finished? What's observably true then?"); in every turn that had an answer to record, the edits to `.specloop/interview.md` (and `product.md`/`AGENTS.md`) came before the next question; it opened follow-up dimensions on its own for named-but-unspecified nouns (`local-json-location`, `book-fields`) and asked them before the closing sweep; Phase A ran to completion including the sweep and moved to Phase B. Also confirmed: "pause" made it ask whether to write `planning/handoff.md` and write nothing; "keep going" resumed at the right dimension; "no sé" on `runtime` triggered a web search and five ranked options, leaving the dimension `open` (unlike Codex and Copilot it did not note the options in the ledger row). Cost: no usage counter is exposed; about 20 prompts on the Free tier with no quota message and empty stderr. Not exercised: Phases C onward, running without `--force`, and a real person's cadence. Evidence saved outside the repo in the owner's harness guide.
- [x] T002 [human] [status:done] Same four checks as T001, for Codex CLI.
      └─ Agent-driven run, 2026-09-19 — all four checks pass. Closed by the owner on 2026-09-19 under the amended `design.md` (an agent-driven non-interactive run counts as `verified`). Codex CLI 0.155.0, isolated fixture with its own `.git` and a `skills/` copy in `.agents/skills/`, driven turn by turn with `codex exec` / `codex exec resume --last`; user's own `sandbox_mode = "danger-full-access"` (an explicit `-s workspace-write` made Windows reject every process spawn — harness limit, not a skill defect). Discovery: pass. Auto-trigger: pass — "where are we? give me the status of this project's roadmap" activated `status`, and "I need to set up a new project and get it organized from scratch." activated `start`, neither naming a skill. `when_to_use` tolerance: pass — all nine skills loaded; the only skill-load error in stderr, every turn, was an unrelated user skill (`hastati-frontend`, missing frontmatter). Multi-turn write-as-you-go: pass — 13 turns; every turn's final message held exactly one question; each answer was written to `.specloop/interview.md`, `planning/product.md` and `AGENTS.md` before the next question; Phase A ran to completion (8 dimensions plus a follow-up dimension the interview opened itself for an unspecified "simple stats"), closing sweep included, then moved to Phase B. Also confirmed: "let's pause" made it ask whether to write `planning/handoff.md` before stopping, "keep going" resumed at the right dimension, and "no sé" on `runtime` triggered a web search plus three ranked options in the user's language, leaving the dimension `open`. Not exercised: Phases C onward, and a real human's typing cadence.
- [x] T003 [human] [status:done] OpenCode audit (fixture: standalone `opecode-test/`, own git boundary, `.agents/skills/` copy). Discovery/auto-trigger: pass — `start` triggered unprompted from "I need to set up a new project and get it organized from scratch," no skill/specloop named. `when_to_use` tolerance: pass — no errors/warnings, skill loaded and ran normally. Write-as-you-go: pass — each answer read-then-edited into `AGENTS.md`/`planning/product.md`/`.specloop/interview.md` before the next question, confirmed through at least 5 questions of Phase A without turns collapsing or batching. Scaffolding (Phase 0 detect-state → Phase 1 base files) matched `start`'s documented phases exactly. First attempt at `specloop/test/opencode-audit/` was discarded — no separate `.git`, so OpenCode's walk-up read specloop's own real `AGENTS.md`/`planning/*` instead of the target's; moved fixture outside the repo entirely to fix.
- [x] T004 [agent] [status:done] OpenCode: `when_to_use` tolerance confirmed pass (T003) — no trim needed. Cursor/Codex CLI results still open (T001/T002) but don't block this task per `requirements.md`'s acceptance criteria, which only requires one harness's result.
- [x] T005 [agent] [status:done] Document the install path for a harness that doesn't read `.claude-plugin/plugin.json`: point it at (or copy `skills/` into) its own skills-scan directory — `.agents/skills/` as the vendor-neutral default, `.opencode/skills/` and `.claude/skills/` named as equivalent aliases. Add to `README.md`'s `## Install` section.
- [x] T006 [agent] [status:done] Generalize the skill-recommendation step off "Claude Code skills/plugins": `skills/start/SKILL.md` Phase 4 step 1, `skills/start/references/question-bank.md`'s Phase C `helper-skills` row, and `README.md`'s Quickstart line 62 phrasing.
- [x] T007 [agent] [status:done] Extend `scripts/check-skill-consistency.mjs` with a check that `skills/start/SKILL.md`'s helper-skills step no longer hardcodes "Claude Code" (the `.claude-plugin/plugin.json` / `claude --plugin-dir` distribution mentions elsewhere in the file are fine and excluded).
- [x] T008 [agent] [status:done] Updated `planning/architecture.md`'s Container section to name OpenCode as verified (and how), Cursor/Codex CLI as not yet audited. All three acceptance criteria in `requirements.md` are now met (one harness verified, install path documented, Container section updated) — `planning/roadmap.md`'s `022` row flipped to `done`. T001/T002 (Cursor, Codex CLI) left `todo` below as optional follow-up for broader confidence, not blocking closure.
- [x] T009 [agent] [status:done] Add a support-matrix table to `README.md`'s `## Install`: one row per roster harness in `requirements.md` (6 rows: Claude Code, OpenCode, Codex CLI, Cursor, Antigravity CLI and GitHub Copilot), columns harness / state (`pending` · `documented` · `verified` · `discarded`) / skills scan path / evidence pointer. Prefill Claude Code and OpenCode `verified` (native host / `T003`), Codex CLI per `T002`'s note, everything else `pending`.
      └─ Table added as `### Support matrix` under `README.md`'s `## Install` (6 rows). Claude Code and OpenCode prefilled `verified`, Codex CLI `pending` per `T002`'s note; the step-1 results from `T010` and `T012` were filled into the same pass.
- [x] T010 [agent] [status:done] Step 1 for Cursor and Antigravity CLI (`design.md` item 1a): from each harness's official docs, cite whether a CLI exists and which directory it scans for skills; set its README matrix row to `documented` or `discarded` (reason stated).
      └─ Cursor: `documented` — scan paths from Cursor's skills docs, CLI command `agent` from its CLI docs; neither page says the CLI loads skills, so `T001` must confirm that. Antigravity CLI: `documented` — command `agy`, project skills in `.agents/skills/` per Google's skills codelab, global path not found; its own `--help` shows `-p/--print` with `--output-format text|json|stream-json`, `-c/--continue`, and `--disable-slash-commands` ("slash command and skill expansion in print mode"); login is a Google browser sign-in; whether a free Google account works is not stated by Google's docs (third-party sources say yes, with tight weekly limits).
- [x] T012 [agent] [status:done] Step 1, same as T010, for GitHub Copilot.
      └─ `documented` at the time — it has a CLI according to its own docs. Gaps to close in the audits: Copilot's page doesn't name the CLI command (`gh skill` installs skills). Caveat on every step-1 result, T010 and T012: read through a page-fetch summarizer, not verbatim; a few summaries carried text foreign to the page and were discarded or refetched, so each audit task should re-check its scan path against the real docs.
- [x] T013 [human] [status:done] Antigravity CLI (`agy`): the four checks from `T001`, per `design.md` item 1a (isolated fixture with its own `.git`; non-interactive run if it loads a full `SKILL.md`, otherwise an interactive run). Record the evidence as a note here and set its README matrix row to `verified`, or `discarded` with the reason (no CLI, no skills support, or not obtainable/runnable here).
      └─ Agent-driven run, 2026-09-19 — all four checks pass, with two harness-specific findings. Antigravity CLI 1.2.7 (`agy`), signed in with a Google account by the owner; isolated fixtures with their own `.git` and a `skills/` copy in `.agents/skills/`; `agy -p` with `--add-dir <fixture>` (required, see Discovery), `--sandbox`, and either `--mode accept-edits` (turns 2–13) or `--dangerously-skip-permissions` (turn 1 and one status run); conversation continued with `--conversation <id>` taken from the `init` event. Discovery: pass with `--add-dir` — asked, without tools, for the skills already injected into its context, `agy -p` alone lists only its 2 built-ins, and with `--add-dir <fixture>` it lists all nine from `.agents/skills/` plus the built-ins; its docs say workspace customizations are found by walking from the cwd up to the repo root, but in `-p` mode the workspace has to be declared. Auto-trigger: pass — with `--add-dir`, "where are we? give me the status of this project's roadmap" made it read `.agents/skills/status/SKILL.md` first, probe `python3` then `python`, run the script by its absolute path and print a correct summary of the same 31998-byte dashboard as the other harnesses; "I need to set up a new project and get it organized from scratch." activated `start`, neither naming a skill (a first status run without `--add-dir` produced the same dashboard, but only after the agent searched the filesystem for context, so it is not counted as discovery). `when_to_use` tolerance: pass — all nine skills injected, no skill-related warning in agy's own log, empty stderr. Multi-turn write-as-you-go: pass — 13 turns; one question per turn (turns 4 and 5 asked one dimension in two sentences); each answer was recorded in `.specloop/interview.md` (and `product.md`/`AGENTS.md`) before the next question through Phase A; a "pause" made it ask whether to write `planning/handoff.md` and write nothing; "keep going" resumed at the right dimension; "no sé" on `runtime` triggered `search_web` and four ranked options, leaving the dimension open. Deviations from the other harnesses: it did not record the closing sweep or create Phase B rows in the ledger (no `runtime` row). Isolation warning: with `--dangerously-skip-permissions`, `--sandbox` did not stop reads outside the workspace, and in two runs the agent explored outside the fixture (home directory, other repos, its own past conversation transcripts, another audit's evidence, shell history); those runs were discarded and redone, the driver now flags any tool path outside the fixture and defaults to `--mode accept-edits`, and all 13 recorded turns had zero outside accesses. Cost: about 703k input / 21k output tokens for the interview and 181k / 15k for `status` (per-response usage summed; input re-sends context), no quota message. Not exercised: Phases C onward, interactive mode (whether the cwd counts as the workspace there), and a real person's cadence. Evidence saved outside the repo in the owner's harness guide. Follow-up 2026-09-19 (agy 1.2.7, after the audit, throwaway fixture): as a loop master it read `skills/loop`, the config, roadmap and task files, wrote `Stage: looping` and `[status:in_progress]`, and dispatched a native sub-agent (`invoke_subagent`, role "Task Runner") with the loop's briefing; without an allow rule that sub-agent died at its first shell command (`Get-ChildItem`, denied) and the master's next response degenerated into one repeated token. With `permissions.allow` = `command(regex:...)` in `~/.gemini/antigravity-cli/settings.json` the same task wrote `hello.txt`, both through the subprocess form with the absolute path substituted and through the sub-agent, so allow rules are honored in headless mode (a plain `command(<text>)` matched only the whole command). In `-p` the master's turn ended right after the dispatch, and the two continuation turns failed with HTTP 429 (`Individual quota reached`, reset in about 161 h), so closing the task (verify, log, roll-up) and the `{repoRoot}` substitution were not exercised (agy used its native sub-agent, never the subprocess path).
- [x] T026 [human] [status:done] GitHub Copilot: same as T013 — four checks, evidence note, README matrix row to `verified` or `discarded` with reason.
      └─ Agent-driven run, 2026-09-19 — all four checks pass. Copilot CLI 1.0.86; isolated fixtures with their own `.git` and a `skills/` copy in `.agents/skills/`; `copilot -p` with `--allow-all-tools` (required in non-interactive mode), conversation continued by named session (`-n` on turn 1, `-r <name>` after); account signed in by the owner. Discovery: pass — `copilot skill list` shows all nine under "Project skills", read from `.agents/skills/`. Auto-trigger: pass — "where are we? give me the status of this project's roadmap" activated `status`, and "I need to set up a new project and get it organized from scratch." activated `start`, neither naming a skill; `status` probed `python3` then `python`, ran the script by its absolute `.agents/skills/` path (Copilot injects a "Base directory for this skill") and wrote the same 31998-byte dashboard as the other harnesses. `when_to_use` tolerance: pass — all nine skills listed, no frontmatter warning in the debug logs (one unrelated runtime WARNING, "event was not delivered to its registered session host"). Multi-turn write-as-you-go: pass — 13 turns; every final message held exactly one question; from turn 2 each answer was written to `.specloop/interview.md` (and `product.md`/`AGENTS.md` where relevant) before the next question; Phase A ran to completion including the closing sweep, then moved to Phase B. Differences from Codex/OpenCode: on turn 1 it asked the first question before scaffolding (nothing to lose yet; it kept scratch state in its own SQL tool) and scaffolded after the first answer; it did not open a follow-up dimension for the unspecified "simple stats" the way Codex did. Also confirmed: a "pause" made it ask whether to write `planning/handoff.md` and write nothing; "keep going" resumed at the right dimension; "no sé" on `runtime` triggered web fetches (python.org, nodejs.org, go.dev) and four ranked options in Spanish, leaving the dimension `open`. Cost: the cumulative AI Credits counter reached 4.17 after the 13 turns, plus 0.8 for the `status` run. Noise, none affecting results: 12 HTTP 422 responses from Copilot's Task API for the named session and 12 hook-reload warnings. Not exercised: Phases C onward, and a real person's cadence. Evidence saved outside the repo in the owner's harness guide.
- [x] T028 [agent] [status:done] Reduce `planning/architecture.md`'s Container section to a pointer at `README.md`'s support matrix (no per-harness names repeated) and update the README passages that still say Cursor/Codex CLI are "not yet audited" (`## What this is`, the closing paragraph of `## Install`) to point at the matrix instead.
      └─ `planning/architecture.md`'s Container section no longer names any harness or verification state — generic "growing list of agent CLIs and IDEs" plus a pointer at `README.md`'s `### Support matrix` (the stray line break inside `.claude-plugin/plugin.json` there is gone too). `README.md`'s `## What this is` and the closing paragraph of `## Install` now point at the matrix instead of saying Cursor/Codex are "not yet audited". Swept the repo for the old claim: `AGENTS.md`'s `022` sentence ("Container section names it") updated; `planning/product.md`'s pointer to Container/`022` still holds and names harnesses only as examples, left as is; `planning/handoff.md` is point-in-time and left as is.
- [x] T029 [agent] [status:done] Acceptance verification against `requirements.md`: no README matrix row is `pending` or `documented`; every `verified` row cites its evidence note; every `discarded` row states a reason; the matrix is the only per-harness list; `node scripts/check-skill-consistency.mjs` and `node scripts/check-markdown-conventions.mjs` pass.
      └─ Verified 2026-09-19. Matrix: 6 rows, all `verified`, none `pending`/`documented`, no `discarded` row (a harness that couldn't be run here was removed from the roster by amendment, not kept as `discarded`). Every row cites evidence — `T003`, `T002`, `T001`, `T013`, `T026`, and Claude Code via `001` T030, `006` T010 and `024` (its row said only "native host" until this pass). The matrix is the only per-harness state list: `architecture.md`'s Container section points at it, and README's install sections, `skills/loop-setup`'s known-flags map, `examples/loop.config.sample.json` and `skills/status`'s script-probe list carry commands, flags and paths, not state; `scripts/check-skill-consistency.mjs` group `[14]` now fails when any of them drifts from the matrix. Both scripts pass. Same-day alignment pass found and fixed: `status`'s script-probe list lacked the Copilot, Cursor and Claude-global paths (an install under `.github/skills/` or `.cursor/skills/` couldn't have found its script); wording that predated the interactive loop (`orchestrator`) and leaked task ids in `skills/start`, `advance` and `task-breakdown`; the example `design.md` used non-canonical headers; the README's `agy` limits were too optimistic. Worker-style check (the loop's `<cli> <args> "<briefing>"` form, one create-a-file task): `copilot` and `cursor-agent` wrote the file; `agy` exited 0 with nothing written, since headless mode auto-denied the shell command the agent used — documented in `README.md` and `skills/loop-setup`. Not exercised anywhere: `skills/loop` as master under a non-Claude harness.
