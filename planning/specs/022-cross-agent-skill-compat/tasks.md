# 022 — cross-agent-skill-compat — Tasks

Status legend: `todo` · `in_progress` · `blocked` · `interrupted` · `done`
Owner: `agent` (loop-runnable) · `human` (skipped by the loop)

- [ ] T001 [human] [status:todo] Cursor: install specloop's skills, confirm discovery of `skills/<name>/SKILL.md`, confirm auto-trigger from a natural-language request matching a skill's `description` (unmodified frontmatter), confirm the harness tolerates the `when_to_use` key, and run `/specloop:start` far enough to confirm the one-question-at-a-time write-as-you-go loop survives a full phase. Record pass/fail per `design.md`'s audit matrix.
- [ ] T002 [human] [status:todo] Same four checks as T001, for Codex CLI.
      └─ Agent-driven run, 2026-09-19 — all four checks pass, but NOT a live human session (`design.md` defines these as human judgment), so left `todo` for the owner to decide whether it closes this. Codex CLI 0.155.0, isolated fixture with its own `.git` and a `skills/` copy in `.agents/skills/`, driven turn by turn with `codex exec` / `codex exec resume --last`; user's own `sandbox_mode = "danger-full-access"` (an explicit `-s workspace-write` made Windows reject every process spawn — harness limit, not a skill defect). Discovery: pass. Auto-trigger: pass — "where are we? give me the status of this project's roadmap" activated `status`, and "I need to set up a new project and get it organized from scratch." activated `start`, neither naming a skill. `when_to_use` tolerance: pass — all nine skills loaded; the only skill-load error in stderr, every turn, was an unrelated user skill (`hastati-frontend`, missing frontmatter). Multi-turn write-as-you-go: pass — 13 turns; every turn's final message held exactly one question; each answer was written to `.specloop/interview.md`, `planning/product.md` and `AGENTS.md` before the next question; Phase A ran to completion (8 dimensions plus a follow-up dimension the interview opened itself for an unspecified "simple stats"), closing sweep included, then moved to Phase B. Also confirmed: "let's pause" made it ask whether to write `planning/handoff.md` before stopping, "keep going" resumed at the right dimension, and "no sé" on `runtime` triggered a web search plus three ranked options in the user's language, leaving the dimension `open`. Not exercised: Phases C onward, and a real human's typing cadence.
- [x] T003 [human] [status:done] OpenCode audit (fixture: standalone `opecode-test/`, own git boundary, `.agents/skills/` copy). Discovery/auto-trigger: pass — `start` triggered unprompted from "I need to set up a new project and get it organized from scratch," no skill/specloop named. `when_to_use` tolerance: pass — no errors/warnings, skill loaded and ran normally. Write-as-you-go: pass — each answer read-then-edited into `AGENTS.md`/`planning/product.md`/`.specloop/interview.md` before the next question, confirmed through at least 5 questions of Phase A without turns collapsing or batching. Scaffolding (Phase 0 detect-state → Phase 1 base files) matched `start`'s documented phases exactly. First attempt at `specloop/test/opencode-audit/` was discarded — no separate `.git`, so OpenCode's walk-up read specloop's own real `AGENTS.md`/`planning/*` instead of the target's; moved fixture outside the repo entirely to fix.
- [x] T004 [agent] [status:done] OpenCode: `when_to_use` tolerance confirmed pass (T003) — no trim needed. Cursor/Codex CLI results still open (T001/T002) but don't block this task per `requirements.md`'s acceptance criteria, which only requires one harness's result.
- [x] T005 [agent] [status:done] Document the install path for a harness that doesn't read `.claude-plugin/plugin.json`: point it at (or copy `skills/` into) its own skills-scan directory — `.agents/skills/` as the vendor-neutral default, `.opencode/skills/` and `.claude/skills/` named as equivalent aliases. Add to `README.md`'s `## Install` section.
- [x] T006 [agent] [status:done] Generalize the skill-recommendation step off "Claude Code skills/plugins": `skills/start/SKILL.md` Phase 4 step 1, `skills/start/references/question-bank.md`'s Phase C `helper-skills` row, and `README.md`'s Quickstart line 62 phrasing.
- [x] T007 [agent] [status:done] Extend `scripts/check-skill-consistency.mjs` with a check that `skills/start/SKILL.md`'s helper-skills step no longer hardcodes "Claude Code" (the `.claude-plugin/plugin.json` / `claude --plugin-dir` distribution mentions elsewhere in the file are fine and excluded).
- [x] T008 [agent] [status:done] Updated `planning/architecture.md`'s Container section to name OpenCode as verified (and how), Cursor/Codex CLI as not yet audited. All three acceptance criteria in `requirements.md` are now met (one harness verified, install path documented, Container section updated) — `planning/roadmap.md`'s `022` row flipped to `done`. T001/T002 (Cursor, Codex CLI) left `todo` below as optional follow-up for broader confidence, not blocking closure.
- [x] T009 [agent] [status:done] Add a support-matrix table to `README.md`'s `## Install`: one row per roster harness in `requirements.md` (17 rows after the 2026-09-19 removal of Factory and Amp — Claude Code, OpenCode, Codex CLI, Cursor, Gemini CLI and the 12 others), columns harness / state (`pending` · `documented` · `verified` · `discarded`) / skills scan path / evidence pointer. Prefill Claude Code and OpenCode `verified` (native host / `T003`), Codex CLI per `T002`'s note, everything else `pending`.
      └─ Table added as `### Support matrix` under `README.md`'s `## Install` (19 rows; 17 after Factory and Amp were removed from the roster, 2026-09-19). Claude Code and OpenCode prefilled `verified`, Codex CLI `pending` per `T002`'s note; the step-1 results from `T010`–`T012` were filled into the same pass.
- [x] T010 [agent] [status:done] Step 1 for Cursor and Gemini CLI (`design.md` item 1a): from each harness's official docs, cite whether a CLI exists and which directory it scans for skills; set its README matrix row to `documented` or `discarded` (reason stated).
      └─ Cursor: `documented` — scan paths from Cursor's skills docs, CLI command `agent` from its CLI docs; neither page says the CLI loads skills, so `T001` must confirm that. Gemini CLI: `documented` — command `gemini`, scans `.gemini/skills/` or the `.agents/skills/` alias (project) and `~/.gemini/skills/` or `~/.agents/skills/` (user); its docs state no headless-mode limit (a third-party claim of one is still unverified — `T013` tests it).
- [ ] T011 [agent] [status:blocked] Step 1, same as T010, for the open-source terminal agents: Mistral Vibe, pi, Hermes Agent, Autohand Code CLI, Deep Code, nanobot, Workshop.
      └─ Six of seven `documented`; Workshop unresolved. The official pages reached (docs.workshop.ai index and "working with the agent"; `llms.txt` is a 404) name neither a CLI command nor a skills path, although agentskills.io lists it as an adopter and describes it as "desktop app, web app, and CLI". Left `pending`; `T020` on a real install resolves it. Notes for later audits: Autohand scans `.autohand/skills/` only (no `.agents/skills/`); nanobot has only a global `~/.nanobot/workspace/skills/` (no project directory), so `T019` can't isolate a per-repo fixture — use a scratch home or accept a global install.
- [x] T012 [agent] [status:done] Step 1, same as T010, for the commercial/enterprise harnesses: Tabnine, Letta Code, Kiro, GitHub Copilot, Junie (Factory and Amp were removed from the roster 2026-09-19 — paid-only, not testable) — the official list does not confirm a CLI for several, so "no CLI" is a valid `discarded` outcome.
      └─ All seven `documented` at the time, none discarded — each has a CLI according to its own docs (Factory and Amp were then removed from the roster, paid-only). Gaps to close in the audits: Copilot's page doesn't name the CLI command (`gh skill` installs skills); Kiro scans `.kiro/skills/` only, not `.agents/skills/`. Caveat on every step-1 result, T010–T012: read through a page-fetch summarizer, not verbatim; a few summaries carried text foreign to the page and were discarded or refetched, so each audit task should re-check its scan path against the real docs.
- [ ] T013 [human] [status:todo] Gemini CLI: the four checks from `T001`, per `design.md` item 1a (isolated fixture with its own `.git`; non-interactive run if it loads a full `SKILL.md`, otherwise an interactive run). Record the evidence as a note here and set its README matrix row to `verified`, or `discarded` with the reason (no CLI, no skills support, or not obtainable/runnable here).
- [ ] T014 [human] [status:todo] Mistral Vibe: same as T013 — four checks, evidence note, README matrix row to `verified` or `discarded` with reason.
- [ ] T015 [human] [status:todo] pi: same as T013 — four checks, evidence note, README matrix row to `verified` or `discarded` with reason.
- [ ] T016 [human] [status:todo] Hermes Agent: same as T013 — four checks, evidence note, README matrix row to `verified` or `discarded` with reason.
- [ ] T017 [human] [status:todo] Autohand Code CLI: same as T013 — four checks, evidence note, README matrix row to `verified` or `discarded` with reason.
- [ ] T018 [human] [status:todo] Deep Code: same as T013 — four checks, evidence note, README matrix row to `verified` or `discarded` with reason.
- [ ] T019 [human] [status:todo] nanobot: same as T013 — four checks, evidence note, README matrix row to `verified` or `discarded` with reason.
- [ ] T020 [human] [status:todo] Workshop: same as T013 — four checks, evidence note, README matrix row to `verified` or `discarded` with reason.
- [ ] T022 [human] [status:todo] Tabnine: same as T013 — four checks, evidence note, README matrix row to `verified` or `discarded` with reason.
- [ ] T024 [human] [status:todo] Letta Code: same as T013 — four checks, evidence note, README matrix row to `verified` or `discarded` with reason.
- [ ] T025 [human] [status:todo] Kiro: same as T013 — four checks, evidence note, README matrix row to `verified` or `discarded` with reason.
- [ ] T026 [human] [status:todo] GitHub Copilot: same as T013 — four checks, evidence note, README matrix row to `verified` or `discarded` with reason.
- [ ] T027 [human] [status:todo] Junie: same as T013 — four checks, evidence note, README matrix row to `verified` or `discarded` with reason.
- [x] T028 [agent] [status:done] Reduce `planning/architecture.md`'s Container section to a pointer at `README.md`'s support matrix (no per-harness names repeated) and update the README passages that still say Cursor/Codex CLI are "not yet audited" (`## What this is`, the closing paragraph of `## Install`) to point at the matrix instead.
      └─ `planning/architecture.md`'s Container section no longer names any harness or verification state — generic "growing list of agent CLIs and IDEs" plus a pointer at `README.md`'s `### Support matrix` (the stray line break inside `.claude-plugin/plugin.json` there is gone too). `README.md`'s `## What this is` and the closing paragraph of `## Install` now point at the matrix instead of saying Cursor/Codex are "not yet audited". Swept the repo for the old claim: `AGENTS.md`'s `022` sentence ("Container section names it") updated; `planning/product.md`'s pointer to Container/`022` still holds and names harnesses only as examples, left as is; `planning/handoff.md` is point-in-time and left as is.
- [ ] T029 [agent] [status:todo] Acceptance verification against `requirements.md`: no README matrix row is `pending` or `documented`; every `verified` row cites its evidence note; every `discarded` row states a reason; the matrix is the only per-harness list; `node scripts/check-skill-consistency.mjs` and `node scripts/check-markdown-conventions.mjs` pass.
