# 017 — project-type-genericity — Tasks

Status legend: `todo` · `in_progress` · `blocked` · `interrupted` · `done`
Owner: `agent` (loop-runnable) · `human` (skipped by the loop)

- [x] T001 [agent] [status:done] Add concrete per-type `planning/architecture.md` header templates to `skills/start/SKILL.md` Phase 1, and fix the stale "Phase 4 fills it" reference
- [x] T002 [agent] [status:done] Add the all-`human`-backlog check to `skills/loop-setup/SKILL.md` Phase 0, before the worker-CLI question
- [x] T003 [agent] [status:done] Add `examples/marketing-content-spec/` (requirements/design/tasks), and list it in `examples/README.md`
- [x] T004 [agent] [status:done] Run the local-only `test/sample-marketing-repo/` fixture (untracked — `test/` is gitignored): scaffold + Phase 2/3 Q&A + roadmap seeding + first-spec requirements against a declared fictional marketing persona
- [x] T005 [agent] [status:done] Close that fixture's first spec design (`specloop:design-closing`) and break it into tasks (`specloop:task-breakdown`)
- [x] T006 [agent] [status:done] Write the fixture's local `NOTES.md` stating the persona and what this run does/doesn't prove
- [x] T007 [agent] [status:done] Flip `006` T011 to `done`, pointing at this fixture
- [x] T008 [agent] [status:done] Verify acceptance criteria: no software dimension appears anywhere in the fixture's `.specloop/interview.md`; `planning/architecture.md` headers match the marketing template
      └─ The "runs with `planning/architecture.md` absent" criterion is verified by code reading only, not a live fixture run against a literally-absent file: `design-closing`/`task-breakdown` Phase 0 already gate on "if they have real content" (conditional, not required) rather than refusing on absence. The local fixture itself has a populated `architecture.md`, so it doesn't exercise the absent case directly. Flagged rather than silently claimed — a real absent-file fixture pass is one line item, not done here.
- [x] T009 [human] [status:done] Live fixture run against a literally-absent `planning/architecture.md`
      └─ Verified 2026-09-11 under a live OpenCode session against `test/architecture-absent-fixture/` (copy of `test/ops-onboarding-repo/` with `planning/architecture.md` deleted entirely, operations/process type). Ran `specloop:design-closing` on spec `002` end to end (Approach → Deliverables → Sequencing → Decisions settled → Open questions → 2 clean closing-sweep rounds) — never refused or got stuck on the file's absence; when the "decisions settled" answer needed recording, it created `planning/architecture.md` from scratch rather than crashing. Then deleted the file again and ran `specloop:task-breakdown` on the same spec — also ran to completion without touching or requiring the file, `tasks.md` populated correctly (6 `agent` tasks + 1 `human` final-approval task). Both halves of the acceptance criterion confirmed.
      └─ **Bonus finding, filed as its own follow-up, not blocking**: when `design-closing` had to create `planning/architecture.md` from scratch, it used generic headers (`Stack & systems`, `Conventions`) instead of the type-keyed template `start`'s Phase 1 defines for operations/process (`Systems`/`Cadence`/`Handoffs`) — `design-closing`'s own `SKILL.md` has no instruction to reuse that template when the file doesn't exist yet. Worth a small fix in `004-design-closing-skill`, not `017`.
