{
  "_comment": "Illustrative only — see examples/README.md. specloop:loop-setup writes the real file to .specloop/loop.config.json in your target repo, from its guided Q&A answers, never hand-authored. Each worker's flag comes from the known-flags map (skills/loop-setup/SKILL.md's known-flags map) — never asked for a known CLI, only for one outside that map.",
  "workers": [
    {
      "cli": "claude",
      "args": [
        "-p"
      ]
    },
    {
      "cli": "codex",
      "args": [
        "exec"
      ]
    },
    {
      "cli": "opencode",
      "args": [
        "run"
      ]
    },
    {
      "cli": "copilot",
      "args": [
        "--allow-all-tools",
        "-p"
      ]
    },
    {
      "cli": "cursor-agent",
      "args": [
        "--trust",
        "--force",
        "-p"
      ]
    },
    {
      "cli": "agy",
      "args": [
        "--add-dir",
        "/absolute/path/to/your/repo",
        "--mode",
        "accept-edits",
        "-p"
      ]
    }
  ],
  "logDir": ".specloop/logs"
}
